---
description: AI Agent Instructions for Aszune AI Bot — see the root CLAUDE.md
globs:
alwaysApply: true
---

# AI Agent Instructions

The authoritative guidance for this repo lives in **[`CLAUDE.md`](../../CLAUDE.md)** at the repo root
(architecture, contracts, testing, toolchain, deployment, and the protected-subsystem rules).

Read that file. This pointer replaces the older, divergent copies that had drifted out of sync on
version numbers, test counts, coverage targets, and tooling.
