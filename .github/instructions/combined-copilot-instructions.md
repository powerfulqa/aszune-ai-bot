# Combined Copilot Instructions → see CLAUDE.md

The authoritative guidance for this repo is **[`CLAUDE.md`](../../CLAUDE.md)** at the repo root. It
consolidates what these instruction files used to say — architecture and the service layer, the
error/testing contracts, the config circular-dependency and triple-export patterns, the Jest
module-mocking gotchas, PM2/systemd guidance, and the protected-subsystem rules — corrected to the
current version, test count, coverage gate, and toolchain.

Read `CLAUDE.md`.
