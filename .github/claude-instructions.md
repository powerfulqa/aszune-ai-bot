# Claude Instructions → see CLAUDE.md

The authoritative guidance for this repo is **[`CLAUDE.md`](../CLAUDE.md)** at the repo root.

This file previously duplicated that guidance with a different (stale) quality/coverage budget. The
single source of truth for the budget is ESLint (`eslint.config.js`, complexity ≤ 15) and for
coverage is `jest.config.js` (global 70%). See `CLAUDE.md`.
