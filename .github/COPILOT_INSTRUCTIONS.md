# Copilot Instructions → see CLAUDE.md

The authoritative guidance for this repo is **[`CLAUDE.md`](../CLAUDE.md)** at the repo root —
architecture, error/testing contracts, toolchain, deployment, and the protected license-enforcement
subsystem rules (`.github/AGENT-SECURITY.md`).

This file previously duplicated that guidance and had drifted out of sync (stale version numbers,
test counts, coverage targets). It now points to the single source of truth.
